/**
 * Shared Claude AI configuration.
 * Change the model ID here to switch all Claude modules at once.
 */

// The model to use for all Claude API calls.
// Update this when the API key has access to more capable models.
export const CLAUDE_MODEL = 'claude-sonnet-4-6';
